{
  "name": "omega-pac",
  "version": "0.0.1",
  "private": true,
  "main": "./index.js",
  "dependencies": {
    "ip-address": "^4.0.0",
    "tldjs": "^1.5.2"
  },
  "scripts": {
    "build": "npm run build:ts && browserify index.source.js --standalone OmegaPac --exclude ip-address -o index.js && browserify index.source.js --standalone OmegaPac | esbuild --loader=js --minify > omega_pac.min.js",
    "build:ts": "tsc -p tsconfig.build.json",
    "dev": "npm link",
    "test": "npm run build:ts && TZ=Europe/London _mocha --reporter spec test/*.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
